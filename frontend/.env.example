# Only needed for a SPLIT deploy (frontend on Vercel, backend on Render).
# Set to the backend's API base URL. Leave unset for local dev (uses the Vite
# proxy to http://localhost:4000).
VITE_API_URL=https://your-backend.onrender.com/api
